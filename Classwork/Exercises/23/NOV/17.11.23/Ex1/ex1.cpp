#include <iostream>
#include <fstream>
#include <string>

using namespace std;

ifstream fin("data.txt");

int main()
{
    char s[n];
    cin.getline(s, 100);
    n = strlen
    for(int i=0; i<; i++)
    {
        if()
    }
    return 0;
}